
## Install dependencies

```
npm install
```

## Run

```
npm start
```

## Build and serve

Install `serve`

```
npm install -g serve
```

Build and serve

```
npm run build
serve -s build # or serve -s build -l host:port
```
